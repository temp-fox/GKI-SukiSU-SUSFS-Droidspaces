# =============================================================================
# 网络功能扩展
#
# 【用途】
# 容器里跑 Docker / UFW / fail2ban / NixOS 等需要高级 iptables 功能的东西。
# 不开也能跑基础容器，所以做成独立开关。
#
# 【已核实】
# 这些在 realme RMX5062 原厂 defconfig 里全部未启用：
#   IP_SET 系列       Kconfig 无 default（等于 n）
#   NETFILTER_XT_*    Kconfig 是 "default m if NETFILTER_ADVANCED=n"，
#                     而 GKI 关掉了模块化，实际等于没有
#
# 注意 Droidspaces 的检查项里并不包含这些 —— 它们是增强，不是必需。
# =============================================================================

# --- ipset ------------------------------------------------------------------
# ipset 是 UFW / fail2ban / 各类防火墙脚本的基础设施

CONFIG_IP_SET=y
CONFIG_IP_SET_MAX=65534

# bitmap 类型：按 IP / IP+MAC / 端口 建集合
CONFIG_IP_SET_BITMAP_IP=y
CONFIG_IP_SET_BITMAP_IPMAC=y
CONFIG_IP_SET_BITMAP_PORT=y

# hash 类型：大规模集合的主力，各种键组合
CONFIG_IP_SET_HASH_IP=y
CONFIG_IP_SET_HASH_IPMARK=y
CONFIG_IP_SET_HASH_IPPORT=y
CONFIG_IP_SET_HASH_IPPORTIP=y
CONFIG_IP_SET_HASH_IPPORTNET=y
CONFIG_IP_SET_HASH_IPMAC=y
CONFIG_IP_SET_HASH_MAC=y
CONFIG_IP_SET_HASH_NET=y
CONFIG_IP_SET_HASH_NETNET=y
CONFIG_IP_SET_HASH_NETPORT=y
CONFIG_IP_SET_HASH_NETPORTNET=y
CONFIG_IP_SET_HASH_NETIFACE=y

# list 类型：集合的集合
CONFIG_IP_SET_LIST_SET=y

# 让 iptables 能用 -m set 匹配 ipset
CONFIG_NETFILTER_XT_SET=y

# --- netfilter 扩展匹配与目标 -----------------------------------------------

# 地址类型匹配（Docker 的 NAT 规则依赖它）
CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y

# 最近连接匹配（fail2ban 的限速规则用）
CONFIG_NETFILTER_XT_MATCH_RECENT=y

# 日志目标（防火墙规则调试）
CONFIG_NETFILTER_XT_TARGET_LOG=y

# --- IPv6 NAT ---------------------------------------------------------------
# 容器出网需要，尤其是纯 IPv6 环境

CONFIG_IP6_NF_NAT=y
CONFIG_IP6_NF_TARGET_MASQUERADE=y
